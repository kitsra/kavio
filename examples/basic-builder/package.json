{
  "name": "@kitsra/kavio-example-basic-builder",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "emit": "node dist/index.js"
  },
  "dependencies": {
    "@kitsra/kavio-builder": "workspace:*"
  }
}

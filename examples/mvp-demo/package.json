{
  "name": "@kitsra/kavio-example-mvp-demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "emit": "node dist/emit-template.js",
    "emit:batch": "node dist/emit-batch.js",
    "emit:expanded": "node dist/emit-expanded.js",
    "prepare-assets": "node dist/prepare-assets.js",
    "render": "node dist/render.js",
    "validate": "node dist/validate.js"
  },
  "dependencies": {
    "@kitsra/kavio-builder": "workspace:*",
    "@kitsra/kavio-render": "workspace:*",
    "@kitsra/kavio-render-worker": "workspace:*"
  }
}
